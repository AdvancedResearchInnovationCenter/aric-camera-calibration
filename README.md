To use a miniconda environment, change the # at the begining of the scripts to call the kernel of your conda environment. Example

```python-repl
#!/home/<home_dir>/miniconda3/envs/<env_name>/bin/python
```
